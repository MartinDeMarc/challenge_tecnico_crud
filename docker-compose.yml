version: '3' 

services:
  backend:
    build:
      context: ./back
      dockerfile: Dockerfile
    ports:
     - "4000:4000"
    environment:
     - DBMONGO=mongodb+srv://martinenriquez12:2oQTvSKOtGrzLcJx@cluster0.k3wuq3s.mongodb.net/crud

  frontend:
    build:
      context: ./front
      dockerfile: Dockerfile
    ports:
      - "4200:4200"
    depends_on:
      - backend 